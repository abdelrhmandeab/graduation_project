# Jarvis — Core Runtime Dependencies
# Install with: pip install -r requirements.txt

# Environment & config
python-dotenv==1.2.2

# Audio pipeline
numpy==2.4.2
sounddevice==0.5.5

# Speech-to-text
faster-whisper==1.2.1

# Wake word detection
openwakeword==0.6.0

# Text-to-speech
pyttsx3==2.99
# Optional neural TTS (edge); runtime falls back to pyttsx3 when missing
# edge-tts>=6.1.12
# Optional edge stream decoder for older edge-tts versions that emit MP3 chunks
# soundfile>=0.12.1

# System monitoring
psutil==7.2.2

# HTTP (used by ollama client & general networking)
httpx==0.28.1
