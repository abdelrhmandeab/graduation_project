# Jarvis — Core Runtime Dependencies
# Install with: pip install -r requirements.txt

# Environment & config
python-dotenv==1.2.2

# Audio pipeline
numpy==2.4.2
sounddevice==0.5.5

# Speech-to-text (cloud-first)
# Local fallback backend
faster-whisper==1.2.1

# Wake word detection
openwakeword==0.6.0

# Text-to-speech
# Neural TTS (edge-tts is the active TTS engine)
edge-tts>=6.1.12
# Edge stream decoder for older edge-tts versions that emit MP3 chunks
soundfile>=0.12.1

# System monitoring
psutil==7.2.2

# HTTP (used by ollama client & general networking)
httpx==0.28.1

# NLP fuzzy matching for noisy STT intent parsing
rapidfuzz==3.14.3
