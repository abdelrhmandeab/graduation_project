# Jarvis — Minimal Dependencies
# Install with: pip install -r requirements-minimal.txt
#
# This file is the ABSOLUTE MINIMUM for Jarvis to start and run.
# Features unavailable at this tier:
#   - Web search (no DDGS) — falls back to "no live data" message
#   - Clipboard read/write
#   - Python brightness control (PowerShell fallback only, may fail without admin)
#   - Semantic intent routing (regex + keyword NLP only)
#   - Outlook email/calendar drafts
#   - Windows Search Index acceleration (directory walk fallback)
#
# For full features, use requirements.txt or requirements-full.txt.

# Environment & config
python-dotenv==1.2.2

# Audio pipeline
numpy==2.4.2
sounddevice==0.5.5

# Speech-to-text — local fallback backend
faster-whisper==1.2.1

# Wake word detection
openwakeword==0.6.0

# Text-to-speech — neural TTS
edge-tts>=6.1.12
soundfile>=0.12.1

# System monitoring (battery/cpu/ram + hardware tier detection)
psutil==7.2.2

# HTTP (Ollama client + general networking)
httpx==0.28.1

# NLP fuzzy matching for noisy STT intent parsing
rapidfuzz==3.14.3
